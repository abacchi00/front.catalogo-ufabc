import { HeaderContainer } from './Header.styles';

const Header: React.FC = () => {
  return (
    <HeaderContainer>
      <h1>Catálogo UFABC</h1>
    </HeaderContainer>
  );
}

export default Header;
